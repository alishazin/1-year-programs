#include <stdio.h>

int main() {
	float PAmount, ROI, TimePeriod, si;
	clrscr();

	printf("\nEnter Principle Amount, Rate of Interest, Time Period (in years) : ");
	scanf("%f %f %f", &PAmount, &ROI, &TimePeriod);

	si = (PAmount * ROI * TimePeriod) / 100;
	printf("\nSimple interest is %0.2f", si);

	return 0;
}